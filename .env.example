# =============================================================================
# Root .env — used by docker-compose.yml
# Copy this file to .env and fill in your own values.
# This file is safe to commit. The .env file is gitignored.
# =============================================================================

# Telegram Bot API Local Server credentials
# Required by the `telegram-bot-api` service in docker-compose.yml
#
# How to get your own API_ID and API_HASH:
#   1. Log in at https://my.telegram.org/apps
#   2. Create a new application (name/platform don't matter)
#   3. Copy the api_id (integer) and api_hash (hex string) below
#
# ⚠️  These are YOUR personal Telegram developer credentials.
#     Do NOT share them. Do NOT commit the actual .env file.
TELEGRAM_API_ID=your_api_id_here
TELEGRAM_API_HASH=your_api_hash_here
